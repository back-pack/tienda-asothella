###############################################################################
#                          Generated on phpdocker.io                          #
###############################################################################
version: "3.1"
services:

    mysql:
      image: mysql:5.7
      container_name: tienda-asothella-mysql
      working_dir: /application
      volumes:
        - .:/application
      environment:
        - MYSQL_ROOT_PASSWORD=root
        - MYSQL_DATABASE=tienda
        - MYSQL_USER=admin
        - MYSQL_PASSWORD=Pass.001
      ports:
        - "3004:3306"

    webserver:
      image: nginx:alpine
      container_name: tienda-asothella-webserver
      working_dir: /application
      volumes:
          - ./public:/application
          - ./phpdocker/nginx/nginx.conf:/etc/nginx/conf.d/default.conf
      ports:
       - "3002:80"

    php-fpm:
      build: phpdocker/php-fpm
      container_name: tienda-asothella-php-fpm
      working_dir: /application
      volumes:
        - .:/application
        - ./phpdocker/php-fpm/php-ini-overrides.ini:/etc/php/7.2/fpm/conf.d/99-overrides.ini

